# 重复与冲突报告

- 基线跟踪文件：49
- 完全重复哈希组：0

## 完全重复文件

未发现完全重复文件。

## 结构性冲突

- `12`中的SQLite＋FTS5＋hash embedding＋RRF实现属于旧检索原型，不作为正式B/D组RAG。
- `03`中的标准卡包含审核逻辑字段，只用于路由和查询扩展，不得直接作为B/D正文。
- `10/.../formal_rag_v2`是历史生成工作包；本次实验使用的脱敏冻结副本统一放在`10/02_RAG冻结快照`。
- 基线历史中曾出现含报告事实的查询快照；当前工作树已脱敏，但历史提交仍需仓库管理员另行评估历史清理。
