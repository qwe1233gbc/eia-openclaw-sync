# 环评审核技能库

本目录将原有 Dify 工作流和审核规则改造成可独立执行的 skill 包。

## 设计原则

- 一个 skill 只审核一个独立条目；
- 各 skill 互不依赖；
- 每个 skill 可单独执行；
- 总 skill 只汇总，不替代单项判断；
- 所有输出统一结构，便于后续 A/B/C/D 消融实验。

## 技能库与法规库的关系

```text
报告证据 + skill 审核流程 + 法规库依据 → 单项审核结果
```

法规库提供依据，skill 提供流程。
