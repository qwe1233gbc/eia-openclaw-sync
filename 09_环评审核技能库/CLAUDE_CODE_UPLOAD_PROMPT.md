请将压缩包中的 `09_环评审核技能库` 覆盖到仓库 `eia-openclaw-sync` 的同名目录，并新建一个分支，例如 `feature/detail-review-skills`。完成后请检查：

1. 每个 `01`–`15` 和 `99` 目录都包含 `SKILL.md`、`config.yaml`、`output_example.json`；其中 `99` 至少包含 `SKILL.md` 和 `config.yaml`。
2. 根目录保留 `README.md`、`skill_registry.yaml`、`common_output_schema.json`。
3. 不修改 `06_Dify工作流` 原始 yml，只把映射关系记录在 `WORKFLOW_MAPPING.md`。
4. 提交信息写：`docs: detail EIA review skill library based on Dify workflows`。
