# 法规库在技能库中的使用方式

各单项 skill 使用法规库时，只能引用与自身审核目标直接相关的条目。

## 使用原则

1. 先看报告证据，再检索法规库；
2. 法规库只提供依据，不替代判断；
3. 若法规库没有对应依据，应输出“无法判断 / 需人工复核”；
4. 不允许编造条款号、限值、参数或标准名称；
5. 不同 skill 不共享中间判断，只共享法规库依据。

## 推荐调用逻辑

```text
输入报告证据
→ 识别审核模块
→ 检索法规库相关条目
→ 对比报告证据与法规要求
→ 输出 skill 结论
```
